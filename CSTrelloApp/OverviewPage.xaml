<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CSTrelloApp.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CSTrelloApp"
    xmlns:data="using:CSTrelloApp.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="ErrorTextBlock" Foreground="Red" Grid.Row="0" TextWrapping="Wrap" />

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- To Do -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="To Do" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ListView x:Name="ToDoList" SelectionMode="None" IsItemClickEnabled="False">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="data:Task">
                            <Border Padding="8" Margin="0,0,0,8" CornerRadius="6" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                                <StackPanel>
                                    <TextBlock Text="{x:Bind Title}" FontWeight="Bold"/>
                                    <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap" MaxLines="2" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                    <TextBlock Text="{x:Bind AssignedTo}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <!-- Doing -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="Doing" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ListView x:Name="DoingList" SelectionMode="None" IsItemClickEnabled="False">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="data:Task">
                            <Border Padding="8" Margin="0,0,0,8" CornerRadius="6" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                                <StackPanel>
                                    <TextBlock Text="{x:Bind Title}" FontWeight="Bold"/>
                                    <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap" MaxLines="2" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                    <TextBlock Text="{x:Bind AssignedTo}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <!-- In Review -->
            <StackPanel Grid.Column="2">
                <TextBlock Text="In Review" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ListView x:Name="InReviewList" SelectionMode="None" IsItemClickEnabled="False">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="data:Task">
                            <Border Padding="8" Margin="0,0,0,8" CornerRadius="6" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                                <StackPanel>
                                    <TextBlock Text="{x:Bind Title}" FontWeight="Bold"/>
                                    <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap" MaxLines="2" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                    <TextBlock Text="{x:Bind AssignedTo}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <!-- Done -->
            <StackPanel Grid.Column="3">
                <TextBlock Text="Done" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ListView x:Name="DoneList" SelectionMode="None" IsItemClickEnabled="False">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="data:Task">
                            <Border Padding="8" Margin="0,0,0,8" CornerRadius="6" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                                <StackPanel>
                                    <TextBlock Text="{x:Bind Title}" FontWeight="Bold"/>
                                    <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap" MaxLines="2" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                    <TextBlock Text="{x:Bind AssignedTo}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
